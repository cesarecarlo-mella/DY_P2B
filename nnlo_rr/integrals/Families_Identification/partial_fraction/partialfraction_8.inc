repeat;
id Prop(r1 + r2)*Prop(p1 + p2 + r1)*Prop(p1 + p2 + r2) = +Num(z)*Den(m2+m2*z)*Prop(r1 + r2)*Prop(p1 + p2 + r1)+Num(z)*Den(m2+m2*z)*Prop(r1 + r2)*Prop(p1 + p2 + r2)+Num(z)*Den(m2+m2*z)*Prop(p1 + p2 + r1)*Prop(p1 + p2 + r2);
endrepeat;
