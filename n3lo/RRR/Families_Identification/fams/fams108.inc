id r1.r2*TOPO(108) = +1*Den(2)*TOPO(108)*DD7+1*Den(2)*TOPO(108)*DD11+(-1)*Den(2)*TOPO(108)*DD13+(-1)*Den(2)*TOPO(108)*DD18+Num(1+u)*Den(2*u)*TOPO(108)*DD23+Num(m2*u-m2*z)*Den(2*u*z)*TOPO(108)*1;
id r1.r3*TOPO(108) = +(-1)*Den(2)*TOPO(108)*DD7+(-1)*Den(2)*TOPO(108)*DD11+1*Den(2)*TOPO(108)*DD18;
id r2.r3*TOPO(108) = +1*Den(2)*TOPO(108)*DD13;
id p1.r1*TOPO(108) = +1*Den(2)*TOPO(108)*DD13+(-1)*Den(2)*TOPO(108)*DD19+(-1)*Den(2)*TOPO(108)*DD23+Num(-m2+m2*z)*Den(2*z)*TOPO(108)*1;
id p2.r1*TOPO(108) = +1*Den(2)*TOPO(108)*DD7;
id p1.r2*TOPO(108) = +(-1)*Den(2)*TOPO(108)*DD10+(-1)*Den(2)*TOPO(108)*DD13+1*Den(2)*TOPO(108)*DD19;
id p2.r2*TOPO(108) = +(-1)*Den(2)*TOPO(108)*DD7+(-1)*Den(2)*TOPO(108)*DD11+(-1)*Den(2*u)*TOPO(108)*DD23+Num(m2*z-m2*u)*Den(2*u*z)*TOPO(108)*1;
id p1.r3*TOPO(108) = +1*Den(2)*TOPO(108)*DD10;
id p2.r3*TOPO(108) = +1*Den(2)*TOPO(108)*DD11;
