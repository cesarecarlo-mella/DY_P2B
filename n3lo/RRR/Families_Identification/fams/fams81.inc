id r1.r2*TOPO(81) = +1*Den(2)*TOPO(81)*DD12;
id r1.r3*TOPO(81) = +(-1)*Den(2)*TOPO(81)*DD12+(-1)*Den(2)*TOPO(81)*DD13+Num(1+u)*Den(2)*TOPO(81)*DD22+Num(m2-m2*u*z)*Den(2*z)*TOPO(81)*1;
id r2.r3*TOPO(81) = +1*Den(2)*TOPO(81)*DD13;
id p1.r1*TOPO(81) = +1*Den(2)*TOPO(81)*DD13+(-1)*Den(2)*TOPO(81)*DD19+Num(-u)*Den(2)*TOPO(81)*DD22+Num(-m2+m2*u*z)*Den(2*z)*TOPO(81)*1;
id p2.r1*TOPO(81) = +1*Den(2)*TOPO(81)*DD13+1*Den(2)*TOPO(81)*DD16+1*Den(2)*TOPO(81)*DD18+Num(-u)*Den(2)*TOPO(81)*DD22+Num(-m2+m2*u)*Den(2)*TOPO(81)*1;
id p1.r2*TOPO(81) = +(-1)*Den(2)*TOPO(81)*DD10+(-1)*Den(2)*TOPO(81)*DD13+1*Den(2)*TOPO(81)*DD19;
id p2.r2*TOPO(81) = +(-1)*Den(2)*TOPO(81)*DD12+(-1)*Den(2)*TOPO(81)*DD13+(-1)*Den(2)*TOPO(81)*DD18+Num(u)*Den(2)*TOPO(81)*DD22+Num(m2-m2*u)*Den(2)*TOPO(81)*1;
id p1.r3*TOPO(81) = +1*Den(2)*TOPO(81)*DD10;
id p2.r3*TOPO(81) = +1*Den(2)*TOPO(81)*DD12+(-1)*Den(2)*TOPO(81)*DD16+(-1)*Den(2)*TOPO(81)*DD22+Num(-m2+m2*z)*Den(2*z)*TOPO(81)*1;
