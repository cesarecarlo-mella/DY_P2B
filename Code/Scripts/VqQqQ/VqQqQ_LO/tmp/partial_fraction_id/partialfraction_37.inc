repeat;
id Prop(p1 + r1)*Prop(p1 + r2)*Prop(p1 + p2 + r1)*Prop(p1 + p2 + r2) = +Num(u*z)*Den(m2+m2*u)*Prop(p1 + r1)*Prop(p1 + r2)*Prop(p1 + p2 + r1)+Num(u*z)*Den(m2+m2*u)*Prop(p1 + r1)*Prop(p1 + r2)*Prop(p1 + p2 + r2)+Num(-z)*Den(m2)*Prop(p1 + r1)*Prop(p1 + p2 + r1)*Prop(p1 + p2 + r2)+Num(-z)*Den(m2)*Prop(p1 + r2)*Prop(p1 + p2 + r1)*Prop(p1 + p2 + r2);
endrepeat;
