repeat;
id Prop(p1 + r1)*Prop(p2 + r1)*Prop(p1 + p2 + r1) = +Num(z)*Den(m2)*Prop(p1 + r1)*Prop(p2 + r1)+Num(-z)*Den(m2)*Prop(p1 + r1)*Prop(p1 + p2 + r1)+Num(-z)*Den(m2)*Prop(p2 + r1)*Prop(p1 + p2 + r1);
endrepeat;
