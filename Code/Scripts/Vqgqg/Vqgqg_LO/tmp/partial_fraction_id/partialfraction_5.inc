repeat;
id Prop(p2 + r1 + r2)*Prop(p1 + p2 + r1)*Prop(p1 + p2 + r2) = +Num(u)*Den(m2+m2*u)*Prop(p2 + r1 + r2)*Prop(p1 + p2 + r1)+Num(u)*Den(m2+m2*u)*Prop(p2 + r1 + r2)*Prop(p1 + p2 + r2)+Num(1)*Den(m2)*Prop(p1 + p2 + r1)*Prop(p1 + p2 + r2);
endrepeat;
