off statistics;
#-
#include all_header.h
#include process_header.h
#include ./tmp/Vqq_colour_out.'NLOOP'.dat
#include ./tmp/CVqq_colour_out.'NLOOP'.dat




l interf = ampl0L*Campl0L;
.sort

id Spin(1,p1,?x, p2)*SpinC(1,p2, ?y, p1) = g_(1,p1,?x, p2, ?y);
.sort
id eps(?x) = e_(?x);
tracen,1;
contract;
.sort



id d_(mu?,nu?) = metric(mu,nu);
id e_(mu1?,mu2?,mu3?,mu4?) =  eps(mu1,mu2,mu3,mu4);
.sort


b Sector, Nc,i_,eps,metric, gs, gem, lambda, T, f, s, pol, Nc, Nf;
.sort
format Mathematica;
#write <tmp/interf.m> " AAdag =(%E)" interf
.end

