repeat;
id Prop(p1 + r2)*Prop(p2 + r2)*Prop(p1 + p2 + r2) = +Num(z)*Den(m2)*Prop(p1 + r2)*Prop(p2 + r2)+Num(-z)*Den(m2)*Prop(p1 + r2)*Prop(p1 + p2 + r2)+Num(-z)*Den(m2)*Prop(p2 + r2)*Prop(p1 + p2 + r2);
endrepeat;
