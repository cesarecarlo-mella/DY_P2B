repeat;
id Prop(p1 + r1)*Prop(p2 + r1)*Prop(p2 + r2)*Prop(p1 + p2 + r2) = +Num(z)*Den(m2*u)*Prop(p1 + r1)*Prop(p2 + r1)*Prop(p2 + r2)+Num(-z-u*z)*Den(m2*u)*Prop(p1 + r1)*Prop(p2 + r1)*Prop(p1 + p2 + r2)+Num(-z)*Den(m2)*Prop(p1 + r1)*Prop(p2 + r2)*Prop(p1 + p2 + r2)+Num(z)*Den(m2*u)*Prop(p2 + r1)*Prop(p2 + r2)*Prop(p1 + p2 + r2);
endrepeat;
