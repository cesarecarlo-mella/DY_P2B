from common import *
from pynman.form_helpers import *

# uncomment to run just the output type specified in common
# run_qgraf()

# uncomment to run qgraf for all output types (form, reduze, drawer) one by one
run_qgraf(all=True)
insert_fams_and_layers()

# enter the correct path to sectormappings and uncomment:
if NLoops > 0:
    SM = loadSM(f'../../../FORMfactor/sectormappings/{NLoops}L')
    ZS = makeZS(SM)
    write_zeros_form(ZS, zerosdir)
else:
    with open(zerosdir, 'w') as f:
        f.write('\n')


print('\nINSTRUCTION: You need to run the Reduze shifter on the output file from QGRAF\nand put it in "external/" subdirectory. Then change Ndiags in process_header.h')
